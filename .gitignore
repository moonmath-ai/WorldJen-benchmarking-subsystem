# Local dataset — do not commit (symlink or downloaded folder)
# Populate via: python download_data.py
# or: ln -s /path/to/WorldJen-dataset data
data
data/

# Environment variables — never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Outputs (generated figures — do not commit)
*.png
*.pdf
*.log

# Anonymized human evaluation data lives in data/human_eval/ (downloaded via download_data.py)

# Internal dev tooling — references old canonical paths used during development
check.sh

# Stale local results — canonical copies live in data/ (downloaded via download_data.py)
semantic_adherence/results/

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
